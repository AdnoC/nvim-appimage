#!/bin/sh

# Store the directory the AppImage is mounted to
export APPIMAGEDIR="$PWD"

# Move back to the directory where neovim was invoked
cd "$OWD"

# Run the bundled neovim binary and pass along any args
"$APPIMAGEDIR"/local/bin/nvim "$@"
